@{
    // Force any globally defined layout to be cleared for any views
    // in this folder and it's sub folders as we need to ensure we 
    // don't inherit anything that doesn't have the correct model statement

    Layout = null;
}