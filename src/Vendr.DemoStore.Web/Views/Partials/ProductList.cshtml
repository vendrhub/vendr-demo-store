@inherits Umbraco.Web.Mvc.UmbracoViewPage<IEnumerable<Vendr.DemoStore.Models.ProductPage>>

@if (Model != null)
{
    <div class="flex flex-wrap -mx-3">
        @foreach (var product in Model)
        {
            @Html.Partial("ProductListItem", product);
        }
    </div>
}